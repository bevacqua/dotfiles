# convenience alias
alias zshconfig="$EDITOR ~/.zshrc"
alias zshconfiguser="$EDITOR ~/.zshrc.user"

# TODO
todo () {
    IFS_OLD="$IFS"
    IFS=$'\n'
    touch $(echo ~/Desktop/$@)
    IFS="$IFS_OLD"
}

alias dotfiles="open $DOTFILES"
