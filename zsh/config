# get the dotfiles repo directory
DOTFILES="$(dirname "$(dirname "$(readlink ~/.zshrc)")")"

# configure oh-my-zsh
ZSH=$HOME/.oh-my-zsh
ZSH_THEME="agnork"
DISABLE_AUTO_UPDATE="true"
DISABLE_CORRECTION="true"
COMPLETION_WAITING_DOTS="true"
plugins=(git)

# load oh-my-zsh
source $ZSH/oh-my-zsh.sh

# customize path
export JAVA_HOME=/usr/lib/jvm/java-7-openjdk
export PATH=/usr/lib/lightdm/lightdm:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin:/usr/local/heroku/bin:$JAVA_HOME/bin:$PATH

# set default editor
export EDITOR="subl -w"

# node version manager
[ -s $HOME/.nvm/nvm.sh ] && . $HOME/.nvm/nvm.sh

ZSHFILES="$DOTFILES/zsh"

# OS-specific configuration
[[ $(uname -s) == 'Linux' ]] && source $ZSHFILES/config.ubuntu
[[ $(uname -s) == 'Darwin' ]] && source $ZSHFILES/config.mac

source $ZSHFILES/completion
source $ZSHFILES/aliases

# machine-specific configuration
source ~/.zshrc.user
